FROM node:22-slim

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

COPY tsconfig.worker.json wrangler.jsonc ./
COPY worker/ worker/

EXPOSE 8787

CMD ["npm", "run", "dev:cloudflare", "--", "--ip", "0.0.0.0", "--port", "8787", "--persist-to", "/data"]
