# Base URL for the signaling server (HTTP — WS is derived automatically)
VITE_SIGNALING_URL=http://localhost:4444

# Optional WebRTC ICE configuration. These credentials are for local compose only.
VITE_WEBRTC_ICE_SERVERS=[{"urls":["turn:127.0.0.1:3478?transport=udp","turn:127.0.0.1:3478?transport=tcp"],"username":"cotab","credential":"cotab-dev"}]
